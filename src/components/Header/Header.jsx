import { HeaderBar } from './HeaderBar';
import { HeaderContainer } from './HeaderContainer';
import './Header.css';

export const Header = () => {
  return (
    <header>
      <HeaderBar />
      <HeaderContainer />
    </header>
  );
};
